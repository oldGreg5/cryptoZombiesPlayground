[profile.default]
src = "src"
test = "test"
script = "script"
libs = ["lib"]

[fuzz]
runs = 1000

[invariant]
runs = 256
depth = 50

[profile.ci.fuzz]
runs = 10000

[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"